{
  "name": "zkvote-subgraph",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.78.0",
    "@graphprotocol/graph-ts": "^0.38.2",
    "typescript": "^5.8.3"
  }
}
